{
	"name": "ts0",
	"version": "0.1.0",
	"description": "Simple TypeScript framework with good defaults",
	"type": "module",
	"bin": {
		"ts0": "./dist/ts0"
	},
	"scripts": {
		"build": "node --experimental-strip-types src/cli.ts build",
		"build:prebuilt": "node --experimental-strip-types scripts/build-prebuilt.ts",
		"prepare": "npm run build",
		"prepublishOnly": "npm run build"
	},
	"files": [
		"dist/ts0",
		"src/runtime/fetch-interceptor.js"
	],
	"dependencies": {
		"@types/node": "^22.0.0",
		"esbuild": "^0.28.1",
		"typescript": "^5.7.0"
	},
	"devDependencies": {
		"preact": "^10.25.4"
	},
	"engines": {
		"node": ">=22.0.0"
	},
	"license": "MIT"
}
