// A normal local import: bundled and inlined as usual. Only the specifiers
// listed in `external` stay references -- everything else is unaffected.
export const THEME_NAME = "EXTERNAL_CSS_SAMPLE_THEME";
